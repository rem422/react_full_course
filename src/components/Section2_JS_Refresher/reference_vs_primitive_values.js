let userMessage = 'Hello';
userMessage = 'Hello There!'
console.log(userMessage);

//Arrays are object which are also reference values
// const does not mean that the value can't be edited but the variable can be overwritten
const hobbies = ['Reading', 'Cooking', 'Gardening'];
hobbies.push('Working');
console.log(hobbies);


//Premetive values are copied when passed as arguments
const message = 'Hey There!';
console.log(message);
